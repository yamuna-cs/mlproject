## End to end ml project
1. create git hub account
2. create README.md with project details
3. create .gitignore (to tell Git which files or directories should be ignored and excluded from version control)
4. create requirements.txt
3. create setup.py 
    ( this file is a Python script that acts as the installation and configuration guide for a Python package. It contains metadata like the package name, version, and dependencies, making it possible for tools like pip to install the project correctly)
4. 